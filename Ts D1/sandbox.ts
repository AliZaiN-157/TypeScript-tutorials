let character = 'ali';

let age = 30;

let isBlackBelt = false;

// Data type remain the same for the variable you can't change it type.
// You can the assign value but not the data type.

const circ = (diameter: number) => {
    return diameter * Math.PI
}

console.log(circ(3));